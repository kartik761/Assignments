#include <stdio.h>
 int main () 
{ 	int a,b,c,d;	
 	
printf("Enter a Value for A\n",a); 	
scanf("%d",&a);  	
printf("Enter a Value for B\n",b); 
	scanf("%d",&b); 	 
	//swaping 	
printf ("A=%d, B=%d\n",a,b);
   c=b;
   d=a;
printf("A=%d, B=%d",c,d);
    
return 0; }
